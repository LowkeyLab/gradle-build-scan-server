import { Component, ChangeDetectionStrategy } from '@angular/core';

@Component({
  selector: 'app-scan-list',
  template: `<p>Scan list works!</p>`,
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class ScanListComponent {}
