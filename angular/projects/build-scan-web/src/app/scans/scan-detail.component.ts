import { Component, ChangeDetectionStrategy } from '@angular/core';

@Component({
  selector: 'app-scan-detail',
  template: `<p>Scan detail works!</p>`,
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class ScanDetailComponent {}
