load("@rules_rust//rust:defs.bzl", "rust_library")

rust_library(
    name = "domain",
    srcs = ["lib.rs"],
    visibility = ["//visibility:public"],
    deps = [
        "@crates//:chrono",
        "@crates//:derive_builder",
        "@crates//:uuid",
    ],
)
