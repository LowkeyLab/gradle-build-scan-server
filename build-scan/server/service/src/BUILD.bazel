load("@rules_rust//rust:defs.bzl", "rust_library")

rust_library(
    name = "service",
    srcs = ["lib.rs"],
    visibility = ["//visibility:public"],
    deps = [
        "//build-scan/lib/src:lib",
        "//build-scan/lib/src:models",
        "//build-scan/server/db/src:db",  # keep
        "//build-scan/server/domain/src:domain",  # keep
        "@crates//:anyhow",
        "@crates//:chrono",
        "@crates//:hex",
        "@crates//:sqlx",
        "@crates//:tracing",
        "@crates//:uuid",
    ],
)
