load("@rules_rust//rust:defs.bzl", "rust_library")

rust_library(
    name = "format",
    srcs = ["lib.rs"],
    visibility = ["//visibility:public"],
    deps = [
        "@crates//:serde",
        "@crates//:serde_json",
    ],
)
